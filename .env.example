# Production site URL, used for canonical URLs, sitemap.xml, and robots.txt.
# Falls back to http://localhost:3000 if unset.
NEXT_PUBLIC_SITE_URL=

# Deployment environment: development | staging | production.
# Used to gate internal-only routes and behaviour.
APP_ENV=
